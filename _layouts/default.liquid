<!DOCTYPE html>
<html lang="en">
  {% include head %}
  <body>
    <div class="layout-Content-maxWidth">
      {% include header %}
      <main>
        {{ content }}
      </main>
      {% include footer %}
    </div>
  </body>
</html>
